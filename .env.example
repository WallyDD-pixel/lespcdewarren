# Copier vers .env ou .env.local : `cp .env.example .env` (Unix) ou copie manuelle sous Windows
# Base SQLite locale (chemin relatif à la racine du projet)
DATABASE_URL="file:./prisma/dev.db"

# Session (32+ caractères en prod)
# SESSION_SECRET=
